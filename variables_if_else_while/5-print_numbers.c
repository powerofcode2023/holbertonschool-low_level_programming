#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 mean success
 */
int main(void)
{
int numbers;
for (numbers = 0 ; numbers <= 10 ; numbers++)
{
printf("Single digit numbers are %d", numbers);
}
putchar('\n');
return (0);
}
