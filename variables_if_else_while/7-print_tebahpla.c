#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 mean success
 */
int main(void)
{
char alphabet;
for (alphabet = 'z' ; alphabet <= 'a' ; alphabet--)
{
putchar(alphabet);
}
return (0);
}
