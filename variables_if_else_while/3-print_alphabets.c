#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 mean success
 */
int main(void)
{
char alphabet;
char alphabet2;
for (alphabet = 'a' ; alphabet <= 'z' ; alphabet++)
putchar(alphabet);
putchar('\n');
else if (alphabet2 = 'A' ; alphabet2 <= 'Z' ; alphabet2++)
putchar(alphabet2);
putchar('\n');
return (0);
}
